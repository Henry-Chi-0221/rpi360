# Legacy compatibility fixtures

These three dual-track files reproduce the v1 container format and metadata.
They were previously shortened and retimed by the old showcase tools. Use them
for reader regression tests only, **never as demo production masters**.

Current demo recipes reference the checksum-verified original Pi recordings;
see [the source manifest](../../demos/sources.manifest.json).
